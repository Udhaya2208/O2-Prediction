name: o2_vision
description: O2-Vision - Tree Oxygen Monitor App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  provider: ^6.1.1
  http: ^1.2.0
  google_fonts: ^6.1.0
  geolocator: ^11.0.0
  permission_handler: ^11.3.0
  image_picker: ^1.0.7
  google_mlkit_image_labeling: ^0.12.0
  google_mlkit_object_detection: ^0.13.0
  exif: ^3.3.0
  fl_chart: ^0.66.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
